# Plan

1. Add one focused CSS generator module and assemble it into every surface.
2. Add one dependency-free delegated runtime and package-root exports.
3. Add a dogfooded component specimen to the shared catalog and initializer.
4. Extend static build contracts and real-Chromium behavior checks.
5. Add the downstream-enabling application-layout breakpoint option and make
   resizable-aside discovery survive drawer-first initialization. Pair the
   runtime option with a BF-owned wide-workspace CSS modifier, then extend the
   shared focus-return IDREF across open- and explicit-close origins.
6. Run the focused checks, full repository gates, screenshot QA, and visual
   review; record the clean commit for downstream pinning.

The geometry is expressed in rem so the required default-pixel dimensions also
scale under browser/root zoom. The group uses native buttons; BF owns only
selection/focus presentation and navigation, while consumers observe clicks
and map their own position values.
