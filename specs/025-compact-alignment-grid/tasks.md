# Tasks

- [x] T001 Record direct authorization and the two-consumer evidence.
- [x] T002 Implement public alignment-grid CSS with intrinsic fixed geometry.
- [x] T003 Implement and export `initAlignmentGrids` plus its public type.
- [x] T004 Add the component catalog specimen and demo initialization.
- [x] T005 Add static build and browser behavior coverage.
- [x] T005A Add the configurable application-layout breakpoint and drawer-first
  resizable-aside discovery with transition coverage.
- [x] T005B Add the paired `is-wide-workspace-breakpoint` CSS modifier while
  preserving the default 48rem contract, and test synchronized geometry and
  runtime state at 1024px and 1200px.
- [x] T006 Run focused checks, `npm test`, and `npm run qa:components`.
- [x] T007 Complete browser visual review and record the clean commit SHA in
  the delivery handoff.
