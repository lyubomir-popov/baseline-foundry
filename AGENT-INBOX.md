# Agent inbox

Spec 007 is active on `feat/007-navigation-brand-panel-alignment`. Align the
application navigation brand to the opposing panel inset and breadcrumb text
baseline, release BF, then refresh Diagram Registry without consumer `.bf-*`
CSS.

Preserve `tmp/chevron-audit/`, `tmp/chevron-harness/`, `tmp/vanilla-main/`, and
the duplicate `fix/typography-role-class-precedence` worktree. The sibling
Vanilla checkout has user changes in `yarn.lock`; do not clean or update it.
