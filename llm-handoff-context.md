# LLM Handoff Context

## Repo orientation

| Role | Path |
|------|------|
| Primary | `baseline-foundry` (this repo) |
| Compatibility package | `portable-vertical-rhythm` |
| Canonical design specs | `canonical-specs` |
| Type-scale reference | `docs-typescale` |

## Quick start

```bash
npm install && npm run setup:demo-font && npm run build && npm run demo
```

Demo: `http://127.0.0.1:4174/`

## Current state

Clean sibling to `portable-vertical-rhythm`: real font metrics, editorial-first baseline alignment, Canonical `4/8/16` grid, and a now-explicit independent surface model for `editorial`, `documentation`, `app`, and the non-canonical `os` addendum. The shared CSS no longer treats editorial as the base tier with diffs on top: each built-in surface emits its own complete scoped token set, and `dist/surfaces.json` publishes both the runtime tokens and the stored font-metric data that produced them. `app` now keeps zero-nudge runtime tokens while still retaining its computed font metrics in the manifest, while `os` stays metrics-driven and element-owned at a much denser measure/control scale. All four built-in surfaces use Ubuntu Sans Variable matching canonical weights exactly, and the repo now also ships a dedicated engine-smoke experiment at `dist/experiments/ibm-plex-engine-smoke/`: one generated stylesheet plus one surface manifest carrying two named large-type surfaces, `IBM Plex Sans` as the default and `Ubuntu Sans` as the alternate, both at H1 `8rem / 9rem` and H2 `4rem / 5rem`. `demo/components/engine-smoke.html` still uses the shared page chrome, but its select is now explicitly labeled as a font-surface control rather than pretending to be the global tier selector. The comparison article no longer stops at prose only: `demo/components/engine-illustration.html` is now the static three-way companion page, keeping the same locked-manifest IBM Plex / Ubuntu bundle while adding a page-local raw-metrics lane beside the shipped compensated lane and the demo-only cap lane. A new draft article, `docs/comparing-baseline-alignment-techniques.md`, now records the current repo view of empirical nudges, cap-unit alignment, raw extracted metrics, and compensated metrics, and `README.md` links to it as the baseline-alignment explainer. `docs/TODO.md` now carries the executed Pragma-informed repo-health plan plus the landed downstream follow-up pair for `brand-layout-ops`: the canonical workflow modal shell (`bf-modal.is-workflow`, plus `.is-resizable`) and the shared top-navigation chevron motion cleanup that now keeps closed dropdown toggles pointed down and rotates active toggles upward. The latest cleanup pass also completed the `bf-panel` audit: shared panels now keep the real Vanilla application-layout pieces without the invented border-card treatment, panel chrome comes from the surrounding application layout rather than a reusable shadowed card surface, and `demo/controls.html` no longer dogfoods `bf-panel` as a decorative wrapper. The next internal hardening pass, the surface completeness audit, is now also complete: `config/foundation-theme.json` once again carries the full required layout spacing token set, `src/build.ts` rejects missing or non-finite required numeric layout fields before generation, and `scripts/validate-build.ts` now fails if generated CSS contains `NaN`, closing the silent bad-output path for incomplete scoped surfaces. The latest shared-shell follow-up completed page chrome polish: `demo/page-chrome.css` now keeps the `pc-controls` cluster on one row when desktop space is available, the existing narrow-width fallback still restores wrapping only once the layout genuinely runs out of room, and `scripts/validate-build.ts` now treats that desktop/mobile wrap split as part of the static demo contract. The latest long-form follow-up added `demo/spec/typographic-specimen.html` as a real spec chapter: it is registered in the shared page catalog, linked from the other spec chapters, boots with the shared page chrome and tier switching, and uses a responsive editorial multi-column prose layout that falls back to one column cleanly on narrow widths. The latest parasite sweep is now also complete end-to-end: the final legacy validation aliases (`.has-error`, `.has-success`, `.has-warning`) are gone from the shared component CSS, the last live `has-*` helper classes are gone from the BF demo/runtime surface, and build validation now rejects both categories so the public contract stays on structural selectors plus canonical `is-*` modifiers only. The latest site-shell follow-up unified the standard global tier header across the tiered site, and the latest OS-tier burst finished the last exception: component demos bootstrap from the shared built-in tier stylesheet, the component page chrome uses the same `Editorial / Docs / App / OS` selector as the living spec and examples, `demo/panel.html` now boots through the shared page chrome as the OS addendum page, and the legacy `panel` preset survives only as an alias to the OS output instead of as a separate `ui-*` role config. The latest parity burst now closes the core Vanilla navigation/list/accessibility basics plus the next list/table follow-through and the dropdown navigation slice: `bf-top-navigation` now ships responsive menu, dropdown, and search behavior, `bf-list`, `bf-inline-list`, and `bf-skip-link` have dedicated demos plus build validation, baseline coverage, and skip-link behavior checks, `bf-icon` now ships the shared search/close/chevron set plus success/error-grey state glyphs, `bf-list-item.is-ticked` / `.is-crossed` mirror Vanilla list-state markers, and `bf-table` now supports Vanilla-style icon-placeholder cells. The lingering `l-*` shell compatibility selectors are now gone from the runtime and generated CSS, build validation enforces the bf-only shell surface, and the full `npm test` suite is green again after the resizable-aside runtime now re-syncs `aria-valuenow` from the rendered aside width on first load. `npm run qa:components` is green after the latest list/table/icon/navigation/panel follow-up, the `icon`, `list`, `table`, `top-navigation`, and panel-bearing app-shell pages are all part of screenshot capture plus baseline verification, and the new top-navigation dropdown paths also pass a targeted Playwright verification run. The latest control follow-up completed the broader occupied-block rollout for bordered, padded controls: inputs/selects/buttons and the rest of the control family resolve block padding from real tier nudges in nudged surfaces and fixed fallback padding in `app`; component demos now boot editorial by default; and the app-tier demo shell no longer forces a white surface that hides the baseline grid. Baseline verification now measures occupied block height (`border box + margin-bottom`) consistently, with composite demos narrowed to the actual control surfaces instead of incidental wrappers. A dedicated manual inspection surface now lives at `demo/components/form-atlas.html`: every row starts with a reference paragraph containing `test`, the tick/switch compare row stays compact at `24px` gaps, and the page now also pulls the rest of the same control family into one place for manual inspection: tabs, equal tabs, choice rows, inline options, range, file input, validation, and a manual table specimen alongside the original button/input/select/search/segmented/tick controls. `thead th` now uses bold body text instead of uppercase `h5`, and the standalone `table` page now follows the explicit symmetric row formula that the repo docs describe: one row padding value (`bodyNudge` in nudged surfaces, compact fallback in `app`), a real bottom border inside the row box, and a solved line-height from `rowBlockSize − 2 × rowPadding − borderWidth`. `docs/TODO.md` now records that as the reusable invariant for tables and any other repeated text rows boxed in by separators when `margin-bottom` is unavailable. The atlas still keeps its manual table row outside the baseline gate because that page is meant as a compact visual inspection surface rather than the canonical table verifier. The range follow-up is now complete in both default and tight-space layouts: `src/css-components.ts` supports `.bf-field.is-range` for the inline editorial row and `.bf-field.is-range.is-stacked` for narrow widths, while `.bf-slider` remains the shared track + number-input primitive. The final inline-range fixes were fivefold: the default range field is a strict two-column grid, the default `.bf-slider` is a no-wrap row, `.bf-form-label` now inherits the same body start/end nudges as the paragraph reference, the slider row top-aligns its children so the number input no longer drags the rail downward, and the final rail/thumb rendering now uses a thin native range-input model instead of the removed local visual-shift workaround. The input itself is the `2px` rail, `--bf-slider-track-offset` / `--bf-slider-row-block-size` place that rail on the active body line, the WebKit track stays transparent, and the runtime still drives fill with `--bf-slider-fill-percent`. After the row-alignment and native-input rewrite, the editorial atlas measurements remain `labelVsReference = 0`, `numberVsReference = 0`; the follow-up captures under `tmp/screenshots/slider-inline-post-nudge/`, `tmp/screenshots/slider-visual-offset-check/`, and `tmp/screenshots/slider-geometry-inspect/` plus `tmp/inspect-slider-geometry.ts` confirm the final geometry in both editorial and app. In the latest numeric signoff pass, `trackTransform = none`, `sliderVisualShift = ""`, and `rangeTop - labelTop = --bf-slider-track-offset` in both tiers, which means the rail center sits on the active body line center without a transform hack. The main demo surfaces (`range`, `controls`, `form-atlas`) use the inline layout, while `range`, `form-atlas`, and `narrow-panel` also expose the stacked field variant for constrained widths. The tick/switch follow-up ended up having two separate causes across editorial, documentation, app, and dense surfaces: checkbox, radio, and switch labels were inheriting body typography but not the active body nudge paddings, and the checkbox/radio box plus switch track were still being vertically placed from raw line-height centering instead of the nudged body text line. `src/css-components.ts` now applies `--bf-body-nudge-start` / `--bf-body-nudge-end` to those labels and computes `--bf-tick-box-offset` / `--bf-switch-track-offset` with `alignedVisualStart(...)`, with the switch slider aligned from `flex-start` instead of center. Playwright captures under `tmp/screenshots/form-atlas-tier-inspect-fixed/`, `tmp/screenshots/form-atlas-tier-inspect-visuals/`, `tmp/screenshots/slider-inline-check/`, `tmp/screenshots/slider-inline-recheck/`, `tmp/screenshots/slider-inline-post-nudge/`, `tmp/screenshots/slider-visual-offset-check/`, `tmp/screenshots/slider-geometry-inspect/`, and `tmp/screenshots/slider-stacked-check/` confirm the updated comparison rows line up across the intended surfaces; app remains zero-nudge by design, but its tick/switch visuals now share the same visual-start math. The stacked button proof in `demo/components/button.html` still demonstrates the invariant: actual text baselines step by `~35.97px` (`error ~0.03px`) on the `4px` grid, while each button's occupied block also lands at `~35.97px` (`error ~0.03px`) and each top border offset stays within `0.08px` of the grid. Cold starts should not try to force the raw button border box to a baseline multiple; the invariant applies to the occupied block. `npm run test:components` is now green across the component suite, including `table`, `tabs`, `panel-tabs`, `segmented-control`, `choice-row`, `inline-options`, `range`, `file-input`, `validation`, `checkbox`, `radio`, `switch`, `narrow-panel`, and `form-atlas`.

**Living-spec audit complete.** No `p-*` parasites. No deprecated patterns in demos. No styled `data-*` attributes. The stale follow-up notes for `bf-theme--light`, `bf-panel-logo`, and `bf-u-no-margin.is-bottom` were removed from `docs/TODO.md` after re-checking the current source, and the last live audit cleanup (`bf-label` as an alias for `bf-status-label`) has now been removed from generated CSS. Workflow modal upstreaming for `brand-layout-ops` is landed, the shared top-navigation chevron contract now matches downstream motion, the `bf-panel` audit is complete, the surface completeness audit now hardens config loading against missing numeric layout fields and `NaN` CSS output, the shared page chrome now keeps its control cluster on one row whenever desktop space is available, the new typographic specimen chapter now gives the editorial tier a longer-form responsive prose specimen, the last `.has-*` validation aliases are gone, the last live `has-*` helper classes are gone from the BF demo/runtime surface, the site now presents the same truthful four-tier header path everywhere, including the OS addendum at `demo/panel.html`, and the old blog-only engine-illustration note is now closed by `demo/components/engine-illustration.html`. The remaining backlog is now downstream-demand parity work only.

**Pragma-informed repo health pass complete.** The Pragma comparison plan has been replaced with an executed repo-health plan filtered to non-opinionated improvements. Delivered: `engine` field in `surfaces.json` manifest entries (all surfaces declare `metrics-compensated`); explicit spacing ontology in the architecture docs (element-owned vs container-owned as named concepts); structured invariant testing in `validate-build.ts` (31 named invariant groups, 1516+ total checks with per-group labels); debug overlay extraction into `src/baseline-grid-overlay.ts` as a separable concern exported from the package index; font asset contract documented in the architecture section. Cap-first alignment and container-owned defaults remain non-adopted taste-driven choices.

## Where to look

| What | Where |
|------|-------|
| Active tasks + principles + architecture | `docs/TODO.md` |
| Longer-term stages + parity inventory | `docs/product-roadmap.md` |
| Completed work log | `docs/history.md` |
| User inbox (triage first) | `docs/AGENT-INBOX.md` |

## Key files

| Purpose | File |
|---------|------|
| CSS generators | `src/css.ts`, `src/css-components.ts`, `src/css-grid.ts`, `src/css-app-tier.ts` |
| Build API / pipeline | `src/build.ts` |
| Tier configs | `config/tiers/{editorial,documentation,app,os}.json` |
| Theme build script | `scripts/build-theme.ts` |
| Published surface manifest | `dist/surfaces.json` |
| Test scripts | `scripts/validate-build.ts`, `scripts/verify-component-baselines.ts`, `scripts/verify-component-behavior.ts` |
| Demo entry | `index.html`, `demo/spec-runtime.js` |

## Critical invariants (do not regress)

- Editorial baseline alignment is sacred. Do not reintroduce app-tier nudges without spec decision.
- Controls follow the Vanilla model: symmetric `padding-block = nudge - border`, no explicit `block-size`, and `margin-bottom = compensation + spaceAfter`, so the occupied block (`border box + margin-bottom`) snaps to the grid. See `docs/TODO.md` → `Control baseline-grid invariant`.
- Control density comes from tier tokens, not per-component `.is-dense` rules or target-height vars.
- Layout containers (Stack, Cluster, Stage-shell) own child spacing via child reset. No margin/padding on text elements inside stacks.
- Tier switching = class-based through complete scoped surfaces (`.bf-tier-editorial`, `.bf-tier-documentation`, `.bf-tier-app`, `.bf-tier-os`) on any `.bf-theme` container. No editorial-base diff contract and no stylesheet swapping.
- Component and root typography must read role-scoped vars for the active tier; do not reintroduce frozen editorial literals.
- Cap engine (`.bf-engine-cap`) is demo-only — drifts at larger sizes.
- No `data-*` CSS selectors. No `ui-*` roles. Only `body` + `h1`–`h6` are valid roles.
- Canonical built-in surfaces use Ubuntu Sans; additional fonts must ship as their own metric-derived surfaces, never as CSS-only font swaps on top of existing nudges.
