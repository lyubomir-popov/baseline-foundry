# LLM Handoff Context

## Repo orientation

| Role | Path |
|------|------|
| Primary | `baseline-foundry` (this repo) |
| Primary linked specs | `canonical-spacing-spec` |
| Legacy spec snapshot | `canonical-specs` |
| Compatibility package | `portable-vertical-rhythm` |
| Type-scale spec | `../canonical-spacing-spec/specs/type scale/draft.md` |

## Quick start

```bash
npm install && npm run setup:demo-font && npm run build && npm run demo
```

Demo: `http://127.0.0.1:4174/`

`npm run setup:demo-font` now provisions both Ubuntu Sans and the IBM Plex Sans variable asset required by the default engine-smoke build.

## Current execution note

- 2026-04-20: completed a grid-example alignment pass against the root `../canonical-spacing-spec/specs/grid/draft.md`. `grid-examples.prompt.md` now maps the checklist to the root-draft placeholders, and `examples/grid/nested-grid.html` now shows explicit small, large, and x-large keyline specimens rather than relying on the versioned snapshot wording.

## Current session plan

- Completed this audit split follow-up: extracted the contiguous `bf-icon` family into `src/css-components/icon.ts`, preserved CSS output order in `componentsCss(...)`, migrated the touched icon invariants in `scripts/validate-build.ts` from substring checks to AST assertions, and revalidated with `npm run build:theme` plus `npm run test:build` (`3843` checks green).
- Completed this inbox-driven BF follow-up: drained `AGENT-INBOX.md`, confirmed `--bf-color-text-muted` and `--bf-color-link` were already public, added `--bf-color-positive`, `--bf-color-positive-background`, `--bf-color-negative`, `--bf-color-negative-background`, and `--bf-font-size-small`, and revalidated with `npm test` (`3768` build checks green, component behavior green).
- Completed this dense-icon verification follow-up: added icon-only `bf-button.is-icon` specimens to `demo/components/button.html`, locked them into build validation, and reran `npm run qa:components`; the `button` page recaptured cleanly and baseline verification stayed at `28 checks, 0 failures` across `editorial`, `documentation`, and `os`.
- Completed this BF `main` forward-port slice: moved the remaining top-navigation dropdown/search-toggle end-slot sizing off hard-coded baseline multiples and onto the shared field-token contract, extended `scripts/validate-build.ts` with direct AST checks for those affordances, and revalidated with `npm run build:theme && npm run test:build` (`3670` checks green).
- Completed this BF `main` forward-port slice: wired the pinned-aside resize handle to the shared authoring-accent active/focus tokens, added build validation coverage for that contract, and revalidated with `npm run build:theme && npm run test:build` (`3680` checks green).
- BF `main` source-side forward-port work is now complete enough for downstream resync. The remaining migration work is downstream-only: Diagram Generator first, then Brand Layout Ops and A4 from their reverted checkpoints.
- Completed this BF `main` forward-port slice: moved `bf-search-and-filter` trailing-control sizing onto the field-token contract by deriving the search and clear slots from the new field padding math instead of baseline multiples, extended `scripts/validate-build.ts` with direct AST checks for that path, and revalidated with `npm run build:theme && npm run test:build` (`3640` checks green).
- Completed this BF `main` forward-port slice: added split action-vs-field inline padding tokens to the source contract, updated the main control/search consumers to use the new tokens explicitly, derived `bf-search-box` trailing sizing from the field token, added shared authoring-accent variables for the gold resize/selection chrome, corrected the IBM Plex smoke config to the shipped `.woff` asset, and revalidated with `npm run build:theme && npm run test:build` (`3605` checks green).
- Completed this follow-up slice: extracted the contiguous `search/search-and-filter` block into `src/css-components/search-box-and-filter.ts`, left the shared dismiss-button selectors plus top-navigation search-context overrides in `src/css-components.ts`, migrated the touched search invariants in `scripts/validate-build.ts` to AST assertions, and revalidated with `npm run build:theme` plus `npm run test:build` (`3550` checks green).
- Completed this planning/docs follow-up: decomposed the former repo-wide dense icon/keyline spacing task into three bounded slices in `TODO.md` so the next implementation can start with the leading disclosure controls instead of widening into a cross-family spacing refactor.
- Completed this follow-up slice: closed the stale `bf-button.is-negative` TODO item after confirming the modifier had already shipped, recorded the brand-button strategy as an intentional non-port because Vanilla deprecates `p-button--brand`, and added both `bf-button.is-link` and `bf-button.is-icon` with button-demo/build-validation coverage.
- Completed this follow-up slice: ran `npm run qa:components` for the new wide-capture component pages, fixed the `tiered-list` baseline-coverage omissions the verifier exposed, restored absolute-positioned cross-column divider pseudos for `bf-equal-height-row`, compensated `bf-cta-block.is-bordered` top padding against its border width, and snapped the `stage-shell` / `figure` / `aspect` demos to baseline-based sizes. `npm run verify:components` and `npm run qa:components` are green again.
- Shared-shell backlog update: `a4-generator` now also needs the tranche-2 BF authoring-shell contract (`bf-application.is-top-navigation-shell` with dark top-navigation/dropdown affordances and pinned-aside layout) so it can drop repo-local designer-shell CSS without diverging from `brand-layout-ops`.
- 2026-04-25: continue the example dogfooding cleanup from the current uncommitted grid/spacing pass.
- Completed this follow-up slice: upstreamed the old `.spec-shell` page framing into the shared `bf-page.is-fill` contract, moved the living spec, spec chapters, OS addendum, and controls page onto that BF-owned modifier, updated the shared spec runtime to anchor on `main.bf-page`, and revalidated with `npm run build`, `npm run test:build`, and live browser checks on the home/spec/controls pages.
- Completed this follow-up slice: replaced the last page-local atlas and grid-guide helpers with BF-owned contracts by adding `bf-grid.is-guide` plus `ul/ol.bf-grid` list resets to core grid CSS, rebuilding the component atlas onto BF card-preview primitives driven by `data-component-atlas-item`, removing `pc-grid-guide` from the living spec shell and `demo-index*` from the component shell, and revalidating with `npm run build`, `npm run test:build`, and `npm run qa:components`.
- Completed this follow-up slice: corrected the middle breakpoint specimen label to the full `620 – 1680 px` 8-column range and removed the typographic specimen's remaining `specimen-*` layout helpers by moving that page onto `bf-fixed-width`, `bf-grid-scope`, and `bf-grid` primitives.
- Completed this follow-up slice: restored the living-spec and grid-chapter breakpoint specimens as real resizable container-query demos, moved the width seeds into `demo/spec-shell.css`, removed the static forced-column fallback, and revalidated the rendered top-of-page breakpoint rows against the live demo after `npm run test:build` passed again.
- Completed this follow-up slice: removed the read-only `demo/components/brand-layout-ops-sample.html` surface and its downstream-only shell CSS from `baseline-foundry`, dropped the atlas/catalog/screenshot-route references, and kept the remaining shared-shell backlog focused on BF-owned primitives only.
- Completed this follow-up slice: removed the `.form-atlas*` helper family from `demo/component-shell.css`, added a shared `bf-inline-size` utility for bounded cluster rows, rebuilt `demo/components/form-atlas.html` onto `bf-section.is-shallow` + `bf-cluster` + `bf-inline-size`, and revalidated with `npm run qa:components`, `npm run build`, and `npm run test:build`.
- Completed this follow-up slice: widened Playwright component QA from single-surface checks to real multi-surface verification by driving shared-tier component pages through `editorial`, `documentation`, and `os`, keeping app-authored pages app-only unless they explicitly opt into broader tier coverage, and fixing the newly exposed OS row-math drift in default inline icons and table rows so `npm run verify:components` and `npm run qa:components` both pass again.
- Completed this follow-up slice: replaced the remaining `demo/spec/grid.html` resize fixtures with explicit breakpoint specimen states, deleted the stray `examples/spacing/container-density.html.bak` backup, clarified the default x-small bracket plus `row-gap: 0` grid contract in source/validation, and extended the `canonical-spacing-spec` handoff so the spacing spec's two-dimensional-grid wording now tracks the same decision.
- Completed this follow-up slice: published `docs/spacing-ownership-peer-review.md` as the shareable neutral architecture note, then locked non-app `bf-stack` modifiers back to gapless so only `bf-tier-app` owns stack gap density.
- Focused validation for that slice passed via `npm run build` and `npm run test:build`.
- Completed this slice: removed the remaining generic `example-frame` wrapper uses from the grid examples, moved the column-span specimen bars onto `bf-card`, moved the app-provisions header/status specimen rows onto shared `bf-card` / `bf-cluster` structure, and converted the `app-panels` / `panel-reflow` examples from local shell fixtures to the shared `bf-application` / `bf-navigation` / `bf-main` / `bf-aside` / `bf-panel` contract.
- `demo/example-page.js` now initializes the shared application-layout and panel-drawer runtimes for example pages, and `scripts/validate-build.ts` now asserts the example shell pages keep using those shared primitives.
- Focused validation stayed green throughout (`npm run test:build`), and the broader repo suite (`npm test`) also passed after the shell/runtime changes.
- Latest live-spec drift audit against `canonical-spacing-spec` matches the codebase expectations on both fronts (grid `row-gap: 0` requirement handed off to spec repo, while app-surface `marginBottom` and `spaceAfter` logic was zeroed in the app configuration data).
- Remaining example debt is now downstream shared-shell work rather than local helper cleanup. `demo/spec-shell.css` now only carries the validated `specimen-grid-scope` resize helpers, `demo/component-shell.css` no longer carries the old `range-demo-rail` or `engine-illustration` helper family, and the engine illustration page now renders through BF-owned cards, status labels, inline-size utilities, and the shared baseline-grid overlay. All arbitrary page-shell framing plus the old atlas/grid-guide/form-atlas/range/engine-illustration helper layer has now been ported to BF-owned primitives.

## Source of truth

### Operational files

| Role | Path |
|------|------|
| Workflow rules | `.github/copilot-instructions.md` |
| Resume agent | `.github/agents/agent.md` |
| Handoff / status | `STATUS.md` |
| Active plan | `TODO.md` |
| Roadmap | `ROADMAP.md` |
| History | `HISTORY.md` |
| Inbox | `INBOX.md` |
| Linked specs | `docs/specs.md` |

### Linked specs

| Role | Path |
|------|------|
| Type-scale spec | `../canonical-spacing-spec/specs/type scale/draft.md` |
| Spacing spec | `../canonical-spacing-spec/specs/spacing/draft.md` |
| Grid spec | `../canonical-spacing-spec/specs/grid/draft.md` |
| Supporting article/reference | `../canonical-spacing-spec/specs/typography-article/draft.md` |
| Legacy snapshot/reference | `../canonical-specs/` |

### Precedence

1. Linked specs in workspace repos or explicitly referenced source docs
2. `ROADMAP.md`
3. `.github/copilot-instructions.md`
4. `STATUS.md` and `HISTORY.md`
5. `README.md` and `docs/specs.md`
6. `INBOX.md`
7. Undocumented local implementation details

## Current state

**Architectural audit landed (2026-04-29).** A repo-wide audit was run and the top 5 recommendations were ranked. The execution path picked is `#3 → #2 → #1`: docs first (fast v1.0 unblocker), AST-helper infra second (de-risks #1), then the big css-components.ts split. Status:

**OS-tier consumer contract clarified (2026-04-30).** The supported neutral entrypoint for downstream consumers that class-switch into `.bf-tier-os` is the shared root bundle `baseline-foundry/styles.css`; `baseline-foundry/tiers/os.css` remains available only for consumers that want OS as the unscoped default surface. That follow-up is now fully closed: the legacy `panel` preset/export has been removed, so downstream OS consumers should use either the neutral root bundle plus `.bf-tier-os` or the direct OS tier bundle.

**Tick/switch geometry check corrected (2026-04-30).** The latest control follow-up closed the misleading compare-harness result as well as the visible radio-dot/knob sizing drift. `src/css-components.ts` now uses border-box pseudo-elements for tick/switch visuals and derives the radio inner dot from the real control visual size, while `scripts/compare-controls.ts` now compares switch/checkbox/radio visuals against the active label line center instead of the padded label box center. The current editorial compare run now reports switch, checkbox, and radio at effectively `0px` center delta, which narrows the remaining open control task to the switch-versus-slider track-language decision rather than baseline centering.

**Dense icon spacing work decomposed (2026-05-01).** The former repo-wide dense icon/keyline spacing task is now split into bounded slices in `TODO.md`: leading disclosure controls first, then trailing action slots, then button child-icon spacing. That matches the actual mechanics in the codebase, where accordion/side-navigation disclosure glyphs, search/top-navigation end slots, and button child-icon spacing each use different alignment contracts and should not be forced through one overnight refactor.

**Dense icon spacing slice 1 landed (2026-05-11).** `bf-accordion-tab`, `bf-side-navigation-accordion-button`, and `bf-side-navigation-expand` now share a dedicated disclosure contract: `1rem` chevron sizing plus a `1rem` in-flow text gap for the label-bearing controls. `scripts/validate-build.ts` now asserts that contract directly, and live demo checks on the accordion and side-navigation pages confirmed `16px` computed gaps/chevron boxes.

- **#3 Surfaces manifest schema docs — landed (`8e4554b`).** New `docs/surfaces-manifest.md` covers top-level shape, `engine` enum, `tokens`/`metrics`, font asset contract, stability guarantees, and consumer recipes. README links to it; a `Surfaces manifest docs` invariant in `validate-build.ts` (13 fragment checks) prevents silent drift.
- **#2 CSS AST assertion infra — landed (`3b02c2d`).** `postcss` is now a dev dep. `scripts/css-ast-helpers.ts` ships `parseCss` / `findRule` / `assertRuleHasDecl` / `assertRuleMissingDecl` / `assertRuleExists`. `scripts/validation-assert.ts` extracts the shared `assert` + `getCheckCount` so helpers feed the same per-invariant counter. The `.bf-top-navigation-row` baseline invariant migrated as the proof-of-concept (1 substring → 6 semantic checks per surface). Pattern documented inline in `validateCommonCss`. `npm run test:build` is green at **3172 checks**.
- **#1 `src/css-components.ts` split — latest burst landed.** `src/css-components/` now exists and the monolith has now shed eleven validated slices without changing CSS output order: `button-actions`, `cards-options`, `chip-badge-status`, `cta-figure-aspect`, `icon`, `list-tree`, `panel`, `search-box-and-filter`, `table`, `tabs-choice-breadcrumbs`, and `tiered-list-equal-height-row`. The latest burst extracted the contiguous `bf-icon` block into `src/css-components/icon.ts`, confirming the plain-string module pattern still works cleanly for a token-driven glyph family. The touched icon invariants are now asserted via AST checks in `scripts/validate-build.ts` (`bf-icon` base styling plus semantic/search/rotated modifiers), and the latest validation (`npm run build:theme` and `npm run test:build`) passed with **3843 total checks**. Shared dismiss-button selectors (`.bf-chip-dismiss`, `.bf-search-box-reset`, `.bf-search-and-filter-clear`) and top-navigation search-context overrides intentionally remain in `src/css-components.ts` for now, so the next navigation burst still owns those contextual rules.

With `bf-icon` no longer on the seam list, the remaining seams are: typography roles, layout primitives (stack/cluster/section/page), controls (input/select/range/file-input/tick/switch), the remaining tabs/navigation slices (top/side/pagination), modals/drawers, accordion, tooltip, and contextual-menu. The split pattern remains proven for both static and helper-driven families.

The IBM Plex path is now explicitly constrained to experiments and downstream custom builds. Ubuntu Sans remains the only built-in tier font, there is no first-class BF IBM Plex preset, and the README now points custom-font adopters at consumer-owned config files with `config/experiments/ibm-plex-engine-smoke.json` as the in-repo reference shape rather than implying a shipped `config/ibm-plex-foundry-theme.json` preset.

Clean sibling to `portable-vertical-rhythm`: real font metrics, editorial-first baseline alignment, Canonical `4/8/16` grid, and a now-explicit independent surface model for `editorial`, `documentation`, `app`, and the non-canonical `os` addendum. The shared CSS no longer treats editorial as the base tier with diffs on top: each built-in surface emits its own complete scoped token set, and `dist/surfaces.json` publishes both the runtime tokens and the stored font-metric data that produced them. `app` now keeps zero-nudge runtime tokens while still retaining its computed font metrics in the manifest, while `os` stays metrics-driven and element-owned at a much denser measure/control scale. All four built-in surfaces use Ubuntu Sans Variable matching canonical weights exactly, and the repo now also ships a dedicated engine-smoke experiment at `dist/experiments/ibm-plex-engine-smoke/`: one generated stylesheet plus one surface manifest carrying two named large-type surfaces, `IBM Plex Sans` as the default and `Ubuntu Sans` as the alternate, both at H1 `8rem / 9rem` and H2 `4rem / 5rem`. `demo/components/engine-smoke.html` still uses the shared page chrome, but its select is now explicitly labeled as a font-surface control rather than pretending to be the global tier selector. The comparison article no longer stops at prose only: `demo/components/engine-illustration.html` is now the static three-way companion page, keeping the same locked-manifest IBM Plex / Ubuntu bundle while adding a page-local raw-metrics lane beside the shipped compensated lane and the demo-only cap lane. A new draft article, `docs/comparing-baseline-alignment-techniques.md`, now records the current repo view of empirical nudges, cap-unit alignment, raw extracted metrics, and compensated metrics, and `README.md` links to it as the baseline-alignment explainer. `TODO.md` now carries the executed Pragma-informed repo-health plan plus the landed downstream follow-up pair for `brand-layout-ops`: the canonical workflow modal shell (`bf-modal.is-workflow`, plus `.is-resizable`) and the shared top-navigation chevron motion cleanup that now keeps closed dropdown toggles pointed down and rotates active toggles upward. The latest cleanup pass also completed the `bf-panel` audit: shared panels now keep the real Vanilla application-layout pieces without the invented border-card treatment, panel chrome comes from the surrounding application layout rather than a reusable shadowed card surface, and `demo/controls.html` no longer dogfoods `bf-panel` as a decorative wrapper. The next internal hardening pass, the surface completeness audit, is now also complete: `config/foundation-theme.json` once again carries the full required layout spacing token set, `src/build.ts` rejects missing or non-finite required numeric layout fields before generation, and `scripts/validate-build.ts` now fails if generated CSS contains `NaN`, closing the silent bad-output path for incomplete scoped surfaces. The latest shared-shell follow-up completed page chrome polish: `demo/page-chrome.css` now keeps the `pc-controls` cluster on one row when desktop space is available, the existing narrow-width fallback still restores wrapping only once the layout genuinely runs out of room, and `scripts/validate-build.ts` now treats that desktop/mobile wrap split as part of the static demo contract. The latest long-form follow-up added `demo/spec/typographic-specimen.html` as a real spec chapter: it is registered in the shared page catalog, linked from the other spec chapters, boots with the shared page chrome and tier switching, and uses a responsive editorial multi-column prose layout that falls back to one column cleanly on narrow widths. The latest parasite sweep is now also complete end-to-end: the final legacy validation aliases (`.has-error`, `.has-success`, `.has-warning`) are gone from the shared component CSS, the last live `has-*` helper classes are gone from the BF demo/runtime surface, and build validation now rejects both categories so the public contract stays on structural selectors plus canonical `is-*` modifiers only. The latest site-shell follow-up unified the standard global tier header across the tiered site, and the latest OS-tier burst finished the last exception: component demos bootstrap from the shared built-in tier stylesheet, the component page chrome uses the same `Editorial / Docs / App / OS` selector as the living spec and examples, `demo/panel.html` now boots through the shared page chrome as the OS addendum page, and the removed `panel` preset no longer couples OS consumers to a legacy preset/export path. The latest root-UI typography cleanup also locked a new invariant into the docs and tests: non-heading component chrome stays body-sized across tiers, so chip value separators, badges, and status labels now read the active `body` role instead of borrowing `h5`, and `demo/component-demo.js` cache-busts the built-in tier stylesheet so rebuilt local typescale edits show up in long-lived demo sessions. The latest drift-hardening pass also closed the audit findings: package exports now publish the OS tier artifact subpaths that the README already documented, shipped `surfaces.json` manifests no longer leak build-machine config/baseline paths, generated baseline directories are emptied before rebuilds so stale `panel` JSON artifacts cannot survive, and the Vite demo server now reruns `npm run build:theme` automatically when `config/**/*.json` changes. The latest parity burst now closes the core Vanilla navigation/list/accessibility basics plus the next list/table follow-through and the dropdown navigation slice: `bf-top-navigation` now ships responsive menu, dropdown, and search behavior, `bf-list`, `bf-inline-list`, and `bf-skip-link` have dedicated demos plus build validation, baseline coverage, and skip-link behavior checks, `bf-icon` now ships the shared search/close/chevron set plus success/error-grey state glyphs, `bf-list-item.is-ticked` / `.is-crossed` mirror Vanilla list-state markers, and `bf-table` now supports Vanilla-style icon-placeholder cells. The lingering `l-*` shell compatibility selectors are now gone from the runtime and generated CSS, build validation enforces the bf-only shell surface, and the full `npm test` suite is green again after the resizable-aside runtime now re-syncs `aria-valuenow` from the rendered aside width on first load. `npm run qa:components` is green after the latest list/table/icon/navigation/panel follow-up, the `icon`, `list`, `table`, `top-navigation`, and panel-bearing app-shell pages are all part of screenshot capture plus baseline verification, and the new top-navigation dropdown paths also pass a targeted Playwright verification run. The latest control follow-up completed the broader occupied-block rollout for bordered, padded controls: inputs/selects/buttons and the rest of the control family resolve block padding from real tier nudges in nudged surfaces and fixed fallback padding in `app`; component demos now boot editorial by default; and the app-tier demo shell no longer forces a white surface that hides the baseline grid. Baseline verification now measures occupied block height (`border box + margin-bottom`) consistently, with composite demos narrowed to the actual control surfaces instead of incidental wrappers. A dedicated manual inspection surface now lives at `demo/components/form-atlas.html`: every row starts with a reference paragraph containing `test`, the tick/switch compare row stays compact at `24px` gaps, and the page now also pulls the rest of the same control family into one place for manual inspection: tabs, equal tabs, choice rows, inline options, range, file input, validation, and a manual table specimen alongside the original button/input/select/search/segmented/tick controls. `thead th` now uses bold body text instead of uppercase `h5`, and the standalone `table` page now follows the explicit symmetric row formula that the repo docs describe: one row padding value (`bodyNudge` in nudged surfaces, compact fallback in `app`), a real bottom border inside the row box, and a solved line-height from `rowBlockSize − 2 × rowPadding − borderWidth`. `TODO.md` now records that as the reusable invariant for tables and any other repeated text rows boxed in by separators when `margin-bottom` is unavailable. The atlas still keeps its manual table row outside the baseline gate because that page is meant as a compact visual inspection surface rather than the canonical table verifier. The range follow-up is now complete in both default and tight-space layouts: `src/css-components.ts` supports `.bf-field.is-range` for the inline editorial row and `.bf-field.is-range.is-stacked` for narrow widths, while `.bf-slider` remains the shared track + number-input primitive. The final inline-range fixes were fivefold: the default range field is a strict two-column grid, the default `.bf-slider` is a no-wrap row, `.bf-form-label` now inherits the same body start/end nudges as the paragraph reference, the slider row top-aligns its children so the number input no longer drags the rail downward, and the final rail/thumb rendering now uses a thin native range-input model instead of the removed local visual-shift workaround. The input itself is the `2px` rail, `--bf-slider-track-offset` / `--bf-slider-row-block-size` place that rail on the active body line, the WebKit track stays transparent, and the runtime still drives fill with `--bf-slider-fill-percent`. After the row-alignment and native-input rewrite, the editorial atlas measurements remain `labelVsReference = 0`, `numberVsReference = 0`; the follow-up captures under `tmp/screenshots/slider-inline-post-nudge/`, `tmp/screenshots/slider-visual-offset-check/`, and `tmp/screenshots/slider-geometry-inspect/` plus `tmp/inspect-slider-geometry.ts` confirm the final geometry in both editorial and app. In the latest numeric signoff pass, `trackTransform = none`, `sliderVisualShift = ""`, and `rangeTop - labelTop = --bf-slider-track-offset` in both tiers, which means the rail center sits on the active body line center without a transform hack. The main demo surfaces (`range`, `controls`, `form-atlas`) use the inline layout, while `range`, `form-atlas`, and `narrow-panel` also expose the stacked field variant for constrained widths. The tick/switch follow-up ended up having two separate causes across editorial, documentation, app, and dense surfaces: checkbox, radio, and switch labels were inheriting body typography but not the active body nudge paddings, and the checkbox/radio box plus switch track were still being vertically placed from raw line-height centering instead of the nudged body text line. `src/css-components.ts` now applies `--bf-body-nudge-start` / `--bf-body-nudge-end` to those labels and computes `--bf-tick-box-offset` / `--bf-switch-track-offset` with `alignedVisualStart(...)`, with the switch slider aligned from `flex-start` instead of center. Playwright captures under `tmp/screenshots/form-atlas-tier-inspect-fixed/`, `tmp/screenshots/form-atlas-tier-inspect-visuals/`, `tmp/screenshots/slider-inline-check/`, `tmp/screenshots/slider-inline-recheck/`, `tmp/screenshots/slider-inline-post-nudge/`, `tmp/screenshots/slider-visual-offset-check/`, `tmp/screenshots/slider-geometry-inspect/`, and `tmp/screenshots/slider-stacked-check/` confirm the updated comparison rows line up across the intended surfaces; app remains zero-nudge by design, but its tick/switch visuals now share the same visual-start math. The stacked button proof in `demo/components/button.html` still demonstrates the invariant: actual text baselines step by `~35.97px` (`error ~0.03px`) on the `4px` grid, while each button's occupied block also lands at `~35.97px` (`error ~0.03px`) and each top border offset stays within `0.08px` of the grid. Cold starts should not try to force the raw button border box to a baseline multiple; the invariant applies to the occupied block. `npm run test:components` is now green across the component suite, including `table`, `tabs`, `panel-tabs`, `segmented-control`, `choice-row`, `inline-options`, `range`, `file-input`, `validation`, `checkbox`, `radio`, `switch`, `narrow-panel`, and `form-atlas`.

**Living-spec audit complete.** No `p-*` parasites. No deprecated patterns in demos. No styled `data-*` attributes. The stale follow-up notes for `bf-theme--light`, `bf-panel-logo`, and `bf-u-no-margin.is-bottom` were removed from `TODO.md` after re-checking the current source, and the last live audit cleanup (`bf-label` as an alias for `bf-status-label`) has now been removed from generated CSS. Workflow modal upstreaming for `brand-layout-ops` is landed, the shared top-navigation chevron contract now matches downstream motion, the `bf-panel` audit is complete, the surface completeness audit now hardens config loading against missing numeric layout fields and `NaN` CSS output, the shared page chrome now keeps its control cluster on one row whenever desktop space is available, the new typographic specimen chapter now gives the editorial tier a longer-form responsive prose specimen, the last `.has-*` validation aliases are gone, the last live `has-*` helper classes are gone from the BF demo/runtime surface, the site now presents the same truthful four-tier header path everywhere, including the OS addendum at `demo/panel.html`, and the old blog-only engine-illustration note is now closed by `demo/components/engine-illustration.html`. Playwright component QA now records one report entry per verified component surface rather than one entry per route, so the shared-tier pages are checked directly in editorial, documentation, and OS while app-authored pages remain app-only unless they explicitly opt into broader tier coverage. The active downstream-demand work is now the `brand-layout-ops` shared-shell backlog: tranche 1 is landed (`bf-panel.is-fill` internal scrolling plus top-navigation action-menu rows), the next candidates are an authoring-shell layout variant and a worksurface/document-frame contract, and mega-nav is explicitly deprioritized unless a concrete consumer asks for it again.

**Pragma-informed repo health pass complete.** The Pragma comparison plan has been replaced with an executed repo-health plan filtered to non-opinionated improvements. Delivered: `engine` field in `surfaces.json` manifest entries (all surfaces declare `metrics-compensated`); explicit spacing ontology in the architecture docs (element-owned vs container-owned as named concepts); structured invariant testing in `validate-build.ts` (31 named invariant groups, 1516+ total checks with per-group labels); debug overlay extraction into `src/baseline-grid-overlay.ts` as a separable concern exported from the package index; font asset contract documented in the architecture section. Cap-first alignment and container-owned defaults remain non-adopted taste-driven choices.

## Where to look

| What | Where |
|------|-------|
| Active tasks + principles + architecture | `TODO.md` |
| Longer-term stages + parity inventory | `ROADMAP.md` |
| Completed work log | `HISTORY.md` |
| User inbox (triage first) | `INBOX.md` |
| Linked specs | `docs/specs.md` |

## Key files

| Purpose | File |
|---------|------|
| CSS generators | `src/css.ts`, `src/css-components.ts`, `src/css-grid.ts`, `src/css-app-tier.ts` |
| Build API / pipeline | `src/build.ts` |
| Tier configs | `config/tiers/{editorial,documentation,app,os}.json` |
| Theme build script | `scripts/build-theme.ts` |
| Published surface manifest | `dist/surfaces.json` |
| Test scripts | `scripts/validate-build.ts`, `scripts/verify-component-baselines.ts`, `scripts/verify-component-behavior.ts` |
| Demo entry | `index.html`, `demo/spec-runtime.js` |

## Critical invariants (do not regress)

- Editorial baseline alignment is sacred. Do not reintroduce app-tier nudges without spec decision.
- Controls follow the Vanilla model: symmetric `padding-block = nudge - border`, no explicit `block-size`, and `margin-bottom = compensation + spaceAfter`, so the occupied block (`border box + margin-bottom`) snaps to the grid. See `TODO.md` → `Control baseline-grid invariant`.
- Control density comes from tier tokens, not per-component `.is-dense` rules or target-height vars.
- Layout containers (Stack, Cluster, Stage-shell) own child spacing via child reset. No margin/padding on text elements inside stacks.
- Tier switching = class-based through complete scoped surfaces (`.bf-tier-editorial`, `.bf-tier-documentation`, `.bf-tier-app`, `.bf-tier-os`) on any `.bf-theme` container. No editorial-base diff contract and no stylesheet swapping.
- Component and root typography must read role-scoped vars for the active tier; do not reintroduce frozen editorial literals.
- Non-heading UI chrome is body-sized. Unless a component exposes an explicit heading slot above `h5`, its font-size and line-height must come from the active `body` role rather than using `h5` as a stand-in UI role.
- Cap engine (`.bf-engine-cap`) is demo-only — drifts at larger sizes.
- No `data-*` CSS selectors. No `ui-*` roles. Only `body` + `h1`–`h6` are valid roles.
- Canonical built-in surfaces use Ubuntu Sans; additional fonts must ship as their own metric-derived surfaces, never as CSS-only font swaps on top of existing nudges.
