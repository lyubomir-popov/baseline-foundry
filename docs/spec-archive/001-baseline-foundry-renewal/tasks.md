# Tasks: Baseline Foundry renewal

**Input**: [`spec.md`](spec.md)

**Plan**: [`plan.md`](plan.md)

**Branch**: `feat/001-baseline-foundry-renewal`

## Phase 1: Specification and operating foundation

- [x] T001 Preserve the raw user request in `raw-request.md`.
- [x] T002 Audit Spec Kit, diagram-generator, and diagram-generator-planning.
- [x] T003 Audit Diagram Registry upstream candidates.
- [x] T004 Audit Vanilla article pagination.
- [x] T005 Audit four-tier/OS support parity.
- [x] T006 Create spec, research, plan, contracts, tasks, and quickstart.
- [x] T007 Author the BF constitution, lean `AGENTS.md`, operational index,
      architecture owner, and spec catalog.
- [x] T008 Distil current root planning narratives, preserve durable decisions,
      then remove `ROADMAP.md`, `STATUS.md`, and `HISTORY.md`.
- [x] T009 Reduce `TODO.md`, `AGENT-INBOX.md`, Copilot instructions, resume agent,
      and prompt references to their single-owner roles.

## Phase 2: Reusable Diagram Registry upstream contracts

### User story 1A: Navigation and page composition

- [x] T010 [P] Implement tagged/grid-aligned top navigation in a focused source
      module with responsive BF-only demo coverage.
- [x] T011 [P] Implement the responsive docs/side-navigation composition and
      wide/narrow demo states.
- [x] T012 [P] Implement scoped full-bleed BF page shell/reset and demo it with
      top navigation.

### User story 1B: Content and media patterns

- [x] T020 [P] Implement `bf-tiered-list.is-flush`, `.is-triple`, and the role
      slot with long/narrow specimens.
- [x] T021 [P] Implement orthogonal `bf-aspect.is-4-3` and `.is-contain`.
- [x] T022 [P] Implement `bf-notice` and semantic variants with accessible demo
      guidance.
- [x] T023 [P] Implement the tokenized, element-owned `bf-eyebrow` role.

### User story 1C: Sequence and control compositions

- [x] T030 [P] Fix tabs active-rule geometry while preserving occupied-block
      baseline assertions.
- [x] T031 [P] Implement BF article pagination per the public contract, including
      boundary, long-title, narrow-container, and RTL specimens.
- [x] T032 [P] Implement `bf-control-row` with wrapping, bottom alignment, and
      locally scoped compensation trimming.

## Phase 3: First-class OS and four-tier element ownership

- [x] T040 Make class-switched component variables complete and assert equality
      against direct tier bundles for every built-in tier.
- [x] T041 Remove app zero-nudge generation, direct-child spacing erasure, and
      app-only stack semantic gaps; move app config to metric-derived,
      element-owned spacing.
- [x] T042 Reconcile manifest `marginBottom` semantics with emitted CSS and add
      token-to-CSS validation.
- [x] T043 Align Ubuntu descriptors and implement one truthful supported font
      loading/package contract.
- [x] T044 Export tier registry and manifest types through supported public
      package entry points and add install/import smoke coverage.
- [x] T045 Make required tier/config/export validation data-driven; remove
      duplicate role keys and validate every required finite component field.
- [x] T046 Remove addendum/non-canonical/three-tier/panel-preset stale copy and
      document OS as the fourth first-class tier.
- [x] T047 Expand fresh baseline, behavior, screenshot, and overflow evidence to
      all four supported tiers and fix all failures.

## Phase 4: Integration and regression evidence

- [x] T050 Integrate focused CSS modules in the central component assembly
      without duplicating selectors or tokens.
- [x] T051 Register every new demo in the component index, page catalog,
      screenshot catalog, static validator, and relevant baseline/behavior
      harnesses.
- [x] T052 Run build and static validation; repair all failures.
- [x] T053 Run component baseline and behavior tests in all required tiers;
      repair failures.
- [x] T054 Run screenshot QA and inspect changed surfaces for overflow and visual
      regressions.
- [x] T055 Correct Diagram Registry's stale agent-inbox statement so it records
      BF's element-owned authority and the downstream refresh boundary.

## Phase 5: Adversarial review and owner QA

- [x] T060 Perform an adversarial API/architecture review against every FR and
      record findings in `review.md`.
- [x] T061 Perform keyboard, semantic, RTL, narrow-width, full-bleed, tier-switch,
      and console review on every quickstart route.
- [x] T062 Re-run `npm test` and `npm run qa:components` after review fixes.
- [x] T063 Launch the demo server, open every required example for owner QA, and
      leave the server available with the exact URL/routes.
- [x] T064 Reconcile `AGENT-INBOX.md`, `TODO.md`, `docs/specs.md`, and this task
      list with the verified final state.

## Dependencies and parallel execution

- T007-T009 establish the durable documentation owners but do not block focused
  source work after T006.
- T010-T012, T020-T023, and T030-T032 are independent workstreams when each
  agent stays inside its assigned source/demo files.
- T040-T047 form one core tier workstream and should not be split across agents.
- T050-T055 follow all implementation workstreams.
- T060-T064 are closeout gates and cannot be claimed from partial automation.

## Phase 6: Rendered component quality amendment

- [x] T070 Preserve the owner follow-up verbatim and add rendered acceptance
      requirements for pagination, row geometry, and tagged-brand geometry.
- [x] T071 [P] Measure article-pagination against BF button-with-icon geometry
      in Playwright across tiers, narrow width, focus, and RTL; implement the
      shared-token correction and focused regression coverage.
- [x] T072 [P] Measure top-navigation row/highlight geometry in Playwright;
      remove vertical row padding without weakening target or responsive states.
- [x] T073 [P] Measure and correct the canonical tagged logo in the actual
      Diagram Registry consumer: Ubuntu orange, top attachment, intrinsic tag
      width, intact mark, and optical mark/wordmark alignment.
- [x] T074 Refresh Diagram Registry from generated BF artifacts without local
      overrides and run its static/freshness gates.
- [x] T075 Run focused and full BF suites plus Diagram Registry checks.
- [x] T076 Perform adversarial Playwright QA on the dedicated component pages
      and real consumer at desktop/mobile, including screenshots, computed
      rectangles, focus, accessibility, RTL, overflow, and browser logs.
- [x] T077 Record final evidence in `review.md`, reconcile live status, and
      leave only the relevant dedicated pages open for owner QA.
- [x] T078 Create an isolated Diagram Registry `HEAD^` worktree; capture a
      matched Playwright comparison for the missing tiered-list heading lines,
      repair the owning public contract, and add focused regression coverage.

## Phase 7: Opus adversarial-review remediation

- [x] T079 Document the tagged-logo height dependency in source and the
      always-on agent invariants; make the brand-region default a generated,
      tier-parity-checked public component token.
- [x] T080 Document article-pagination direction modifiers as required,
      order-independent public semantics.
- [x] T081 Add four-tier article-pagination probes at 19rem, 20rem, 21rem, and
      the two-column state, including baseline snapping and overflow.
- [x] T082 Add four-tier tiered-list probes immediately below/at 38.75rem for
      default, full-width, flush, and triple variants.
- [x] T083 Add four-tier equal-height subgrid row measurements; fix the invalid
      self-container query they exposed, then rerun full automated and rendered
      QA.

## Phase 8: Health-audit state closeout

- [x] T084 Reconcile the final health audit against source and tests; remove
      recommendations already satisfied by the public brand-region token,
      downstream refresh, and operational quick reference.
- [x] T085 Drain completed review narrative from `AGENT-INBOX.md` and retire the
      duplicate root audit after preserving durable conclusions in `review.md`.
- [x] T086 Convert genuine demand-gated gaps into unnumbered catalog candidates
      with evidence triggers, without opening another active package on the
      Spec 001 branch.
- [x] T087 Reconcile `TODO.md`, `docs/specs.md`, and the live handoff, then run
      documentation/link and diff-cleanliness checks.

## Phase 9: Vanilla and Sites parity amendment

- [x] T090 Preserve the owner request and no-port decisions verbatim in the
      active package.
- [x] T091 Fetch current Vanilla `main`, preserve the sibling checkout's dirty
      work, and create a clean comparison worktree at the audited revision.
- [x] T092 Audit root SCSS, standalone bundles, layouts, documentation and Sites
      macros; publish the complete selection in the parity contract.
- [x] T093 Correct article pagination to preserve Vanilla's persistent same-row
      structure, logical half placement and semantic spacing while retaining BF
      type roles and baseline-safe occupied geometry.
- [x] T094 Replace the regression-enforcing pagination tests with four-tier
      19rem-to-wide, boundary, source-order, RTL, focus, text-edge, baseline and
      overflow assertions.
- [x] T095 [P] Use Terra agents for the first isolated static-pattern tranche:
      data spotlight, divided section, in-page navigation, logo section, media
      object, reduced navigation and table of contents.
- [x] T096 [P] Use Sol agents for invariant-heavy owner-selected ports:
      credential and validation composition, notifications, and sortable,
      expanding, and mobile-card table behaviours. Keep mega-navigation as
      verification-only and processing-button state demand-gated per contract.
- [x] T097 [P] Use Sol/Terra agents for the first Sites foundation tranche and
      reserve content-card for its own high-risk workstream.
- [x] T098 Integrate shared exports, selectors, routes and generated output only
      after each isolated public contract is reviewed.
- [x] T099 Use Luna agents for mechanical demo/catalog/test fixtures after the
      corresponding API and layout contract is frozen.
- [x] T100 Continue dependency-ordered batches until every planned row in
      `contracts/vanilla-sites-parity.md` is implemented or explicitly recorded
      as an already-covered verification result.
- [x] T101 Run focused static, behaviour, baseline and overflow checks for each
      batch and keep the contract status current.
- [ ] T102 Run final `npm test`, `npm run qa:components`, and integrated-browser
      review across all four tiers and material responsive states.
      Automated gates are green; the integrated-browser backend is unavailable,
      so that explicitly requested visual sign-off remains open.
- [x] T103 Record closeout evidence in `review.md` and reconcile `AGENT-INBOX.md`,
      `TODO.md`, `docs/specs.md`, and this task list.
- [x] T104 Split the demo information architecture into distinct Component and
      Vanilla/Sites Pattern atlases; preserve isolated QA routes, publish the
      complete selected pattern/layout inventory and exclusion dispositions,
      and add static regression coverage for the catalog boundary.
- [ ] T105 Verify the Pattern Atlas route in the explicitly requested in-app
      browser across representative desktop and constrained states. Do not
      substitute another browser surface when the in-app backend is absent.
