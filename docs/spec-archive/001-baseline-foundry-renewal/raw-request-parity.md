# Owner request: Vanilla and Sites parity amendment

> pagination was ported fairly badly - the prev next are not even on the same
> row. we need much stricter porting ensuring the layout is respected, finding
> the closest spacing variables/tokens to what was in the original vanilla,
> but never sacrificing the vertical rhythm that is the raison d'etre of this
> project.
>
> also, i do not see any of the sites patterns for example v - see the local
> vanilla-framework repo in the same parent folder, ppull the latest main to
> compare.

> divider heading-icon matrix should not be ported. the rest from the
> "Excluded or deferred, available to reconsider" list should be ported by
> subagents - you decide whether terra luna or sol. you orchestrate. the
> intentionally superseded and Deprecated upstream; permanently excluded can
> remain excluded.

> write a spec so we ca ncontinue in anohter chat if needed, use the spec-kit
> format, another sibling repo. lets do this in a disciplined way

> use the integrated browser pls

> what about the vanilla patterns? i only see components. so basic section and
> a ton of other layouts/patterns are missing
