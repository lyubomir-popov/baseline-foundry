# Spec archive

Completed or retired Spec Kit packages move here after their matching feature
branch is merged. Archived packages are durable evidence, not active context.

Keep active and draft work under `specs/`. Keep cross-spec execution order in
`TODO.md` and catalog/status in `docs/specs.md`.
